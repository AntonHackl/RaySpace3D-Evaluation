import trimesh
import numpy as np
import sys

# Need to load the raytracer preprocess data or raw dt data to visually test 108 vs 20637
# I will use open3d or trimesh to just see what they form
sys.path.append('src/RaySpace3D/scripts')
# Well dt loader might be harder, let's just make a script to query rayspace exactly on 108, 20637
